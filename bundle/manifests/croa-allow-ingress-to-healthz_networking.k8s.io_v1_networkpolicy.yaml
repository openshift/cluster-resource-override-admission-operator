apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: croa-allow-ingress-to-healthz
spec:
  ingress:
  - ports:
    - port: 8080
      protocol: TCP
  podSelector:
    matchLabels:
      clusterresourceoverride.operator: "true"
  policyTypes:
  - Ingress
